version: '3'

services:
  sb-postgres:
    image: postgres
    expose:
      - 5432
    volumes:
      - sb-postgres:/var/lib/postgresql/data

  reation-roles:
    build:
      context: reation-roles/
      dockerfile: Dockerfile
    expose:
      - 5000
    env_file:
      - reation-roles.env


  discord-bot:
    build:
      context: discord-bot/
      dockerfile: Dockerfile
    env_file:
      - discord-bot.env
  
volumes:
  sb-postgres: